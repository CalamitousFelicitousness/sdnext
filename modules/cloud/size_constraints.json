{
  "schema_version": 1,
  "generated": "2026-05-17T00:00:00Z",
  "_notes": "Entries here override the live extraction in adapter.normalize_models. Add a provider/model entry only when the provider does not advertise resolutions via supported_parameters AND you have authoritative knowledge of the constraint. For NanoGPT and other rich-metadata providers, constraints are extracted live from /v1/image-models on each list_models call; entries here would only be needed to override that live data. The four OpenAI entries below are hardcoded because OpenAI's /v1/models does not include size information.",
  "entries": {
    "openai/dall-e-2": {
      "kind": "enum",
      "options": ["256x256", "512x512", "1024x1024"],
      "default": "1024x1024"
    },
    "openai/dall-e-3": {
      "kind": "enum",
      "options": ["1024x1024", "1024x1792", "1792x1024"],
      "default": "1024x1024"
    },
    "openai/gpt-image-1": {
      "kind": "enum",
      "options": ["1024x1024", "1536x1024", "1024x1536"],
      "allow_auto": true,
      "auto_wire": "literal",
      "default": "1024x1024"
    },
    "openai/gpt-image-1-mini": {
      "kind": "enum",
      "options": ["1024x1024", "1536x1024", "1024x1536"],
      "allow_auto": true,
      "auto_wire": "literal",
      "default": "1024x1024"
    }
  }
}
